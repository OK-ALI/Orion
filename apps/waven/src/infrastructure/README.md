# WAVEN infrastructure adapters

Planned adapters include music providers, artwork and metadata caches, local persistence, Android playback, and Orion Cloud transport. The initial workspace intentionally contains no copied Orion Cinema WebView, NSD, updater, or hard-coded `com.okali.orion` native plugin.
